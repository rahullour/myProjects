<nav class="navbar navbar-expand-lg navbar-light bg-light">

<%= link_to "Alpha Blog" , root_path, class: "navbar-brand", id:"logo" %>

<div class="container-fluid">
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                      <%= link_to "Bloggers" , users_path, class: "nav-link", id: "bloggers_btn" %>
                    </li>
                    <li class="nav-item dropdown">
                          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Articles
                          </a>
                          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                
                                <% if logged_in? %>
                                    <li><a class="dropdown-item" href="#"> <%= link_to "Create new article", new_article_path, class: "dropdown-item" %></a></li>
                                <% end %>  
                                <li><hr style="color:green;" class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="#"><%= link_to "View articles", articles_path, class: "dropdown-item" %></a></li>
                            
                          </ul>
                    </li>
                

                    <li class="nav-item dropdown">
                    <% if logged_in? %>
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <%= "(Admin)" if current_user.admin? %> Profile [<%= current_user.username %>]
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <%= link_to "View your profile", user_path(current_user), class: "dropdown-item" %>
                            <li><hr style="color:blue;" class="dropdown-divider"></li>
                            <%= link_to "Edit your profile", edit_user_path(current_user), class: "dropdown-item" %>
                            <%= button_to 'Delete Profile', user_path(current_user),
                            method: :delete,
                            form: { data: { turbo_confirm: 'Are you sure?' } }, class: "dropdown-item text-danger" %>
                        </ul>
                        <% end %> 
                    </li>

                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Category
                    </a>
                    
                 
                    <% if logged_in? && current_user.admin? %>
                       
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <%= link_to "View categories", categories_path, class: "dropdown-item" %>
                            <li><hr style="color:blue;" class="dropdown-divider"></li>
                            <%= link_to "Create a new category", new_category_path, class: "dropdown-item" %>
                            
                           
                        </ul>
                        <% end %> 


                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <%= link_to "View categories", categories_path, class: "dropdown-item" %>
                        </ul>

                    </li>


                                               
                   <% if logged_in? %>
                        <li class="nav-item">
                          <%= link_to 'Logout', logout_path, class: "nav-link", id: "logout_btn", method: :delete %>
                        </li>
                     
                  <% else %>
                        <li class="nav-item">
                          <%= link_to 'Log in', login_path, class: "nav-link" %>
                        </li>
                        <li class="nav-item">
                          <%= link_to 'Sign up', signup_path, class: "nav-link" %>
                        </li>
                  <% end %>
          
            </ul>
       
    </div>
</div>
</nav>