spring.application.name=wechat

#aws db config
#spring.datasource.url=jdbc:mysql://database-1.czk42yog8v5o.ap-south-1.rds.amazonaws.com:3306/students_tracker
spring.profiles.active=prod

#logging config
spring.main.banner-mode=off
#spring.jpa.hibernate.dll-auto=update
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace
logging.level.org.springframework.jdbc.core=trace
logging.level.org.springframework.security=DEBUG
logging.level.com.creating.chatApplication=DEBUG

# ==========================================
# Server & Session Settings
# ==========================================
server.servlet.session.timeout=90m

# ==========================================
# Optimized Hikari Pool for Render Free Tier
# ==========================================
# Keeping it tight saves RAM and prevents database connection exhaustion
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-timeout=30000

# ==========================================
# File Uploads
# ==========================================
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB

# ==========================================
# Optimized Actuator Config for Quick Startup
# ==========================================
management.endpoints.web.exposure.include=health,info
# 'when-authorized' or 'never' prevents Actuator from pinging the DB during startup health checks
management.endpoint.health.show-details=never
management.info.env.enabled=false

# ==========================================
# Bugs and Features to be done
# ==========================================

#add glass view to the site UI
#move trix toolbar at top of edit/reply preview
#fix ui of attachments previews in toolbar-input-box
#close message-reply-preview/message-edit-preview already open before opening of either one
#invite user btns UI change

#show status of each chat in li below chat names

#test - file attachments flow in edit/reply messages
#group admin and chat deletion flow
#add/edit group members
#for faster editing messages, update the firebase db with only updated data not - clear all and add all approach
#add exit onetooneechat/group feature, delete account feature .
#don't allow click on other chats until one chat load finishes, or find another sol, as this causing mixed messages show in same chat
#after account update from settings, chats email should also get updated, both on local and firebase
#add explicit page for global errors
#invite user accept/reject flow, remove email invite confirm, backend flow will remain same
#docker
#elastic search for search in messages
#after joinee accepts invite, send notfication/email to sender, notification sending is prefered .
#login with google first time , save password to clipboard
