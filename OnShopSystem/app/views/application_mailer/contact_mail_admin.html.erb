<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <%= yield %>
    <h3 style="color:green;"> A Query Has Been Submitted On OnShopSystem ! </h3>
    <b> From:</b><span> <%=Contact.where(id: @contact_id).last.email%></span><br>
    <b> Title:</b><span> <%=Contact.where(id: @contact_id).last.title%></span><br>
    <b> Query:</b><span> <%=Contact.where(id: @contact_id).last.description%></span>
    <p> Team OnShopSystem </p>
  </body>
</html>
